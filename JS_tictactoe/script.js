// const gameBoard;
//
// displayController;

const playerFactory = (name, symbol) => {
  return { name, symbol };
};

const playerOne = playerFactory("andy", "x");
const playerTwo = playerFactory("hal", "o");
